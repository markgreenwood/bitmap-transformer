# Bitmap transformer
## Requirements

## API

```bm.read(filename, [callback])```

Read the bitmap.

```bm.transform(function)```

Transform the bitmap.

```bm.write(filename, [callback])```

Write the bitmap.